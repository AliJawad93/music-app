import 'package:flutter/material.dart';

class AppIcons {
  static const music = Icon(Icons.music_note_outlined);
  static const video = Icon(Icons.video_collection_outlined);
  static const libraryMusic = Icon(Icons.my_library_music_outlined);
  static const settings = Icon(Icons.settings_outlined);
  static const next = Icon(Icons.skip_next);
  static const previous = Icon(Icons.skip_previous);
  static const pause = Icon(Icons.pause);
  static const play = Icon(Icons.play_arrow);
  static const list = Icon(Icons.list);
  static const more = Icon(Icons.more_horiz);
  static const shuffling = Icon(Icons.shuffle);
  static const repeat = Icon(Icons.repeat);
  static const repeatOne = Icon(Icons.repeat_one);
}
